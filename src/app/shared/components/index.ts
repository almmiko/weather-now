import { LogoComponent } from '@shared/components/logo/logo.component';
import { ButtonComponent } from '@shared/components/button/button.component';

export const components: any = [ ButtonComponent, LogoComponent ];
